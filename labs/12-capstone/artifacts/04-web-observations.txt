SIMULATED WEB / API OBSERVATIONS

Public application features observed on shop.umbramarket.lab:

- /catalog
- /login
- /cart
- /checkout
- /account
- /account/orders

After login as alice.demo@example.lab:

GET /account/orders
-> 200 OK
-> page contains order reference #1001

Browser/API traffic generated by the page:

GET https://api.umbramarket.lab/api/orders/1001
Authorization: Bearer <alice-demo-token>

Response:

HTTP/1.1 200 OK
Content-Type: application/json

{
  "orderId": 1001,
  "owner": "alice.demo@example.lab",
  "status": "paid",
  "total": 79.90
}

Additional observation:

The UI only shows Alice's own orders. The object identifier is present in the API path.

QUESTIONS

- What is a fact?
- What authorization hypothesis is reasonable?
- What would be a minimal comparison using the second provided test account?
- What result would disprove the hypothesis?
